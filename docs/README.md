# Documentation

- [`architecture/`](./architecture/README.md) — Clean Architecture rules, the dependency rule, error handling, configuration, DI.
- [`ai/`](./ai/README.md) — the AI layer's structure and current (Sprint 0) status.
- [`presentation-engine/`](./presentation-engine/README.md) — the presentation-engine's structure and current (Sprint 0) status.
- [`development/`](./development/README.md) — setup, scripts, conventions, path aliases.

For folder-level rationale inside `src/`, see the `README.md` alongside
each folder (`src/features/README.md`, `src/services/README.md`,
`src/di/README.md`, `src/components/shared/README.md`, and the
`README.md` files under `src/ai/*` and `src/renderer/*`).
